---
name: Task
about: 実装タスク（Story の Sub-issue）を作成する
title: ''
labels: ''
type: 'Task'
---

## 概要

{このタスクで何をやるか（1〜2文。親 Story の文脈を前提に、このタスク固有のスコープを書く）}

- 親Story: #{番号}
- カバーする親の受入基準: {受入基準1, 受入基準3 等。この Task で担保する親 Story の受入基準を列挙}

## 振る舞い仕様

{このタスクのスコープ内で実現すべき振る舞いを、親 Story の受入基準・パターン一覧から抜粋・具体化する}

| 状況 | 操作 | 期待結果 | 種別 |
|------|------|---------|------|
| {前提条件} | {ユーザーまたはシステムの操作} | {観測可能な結果} | 正常 / 異常 |

<!-- エラーケース・境界条件を必ず含める -->
<!-- 「エラーケースの詳細こそが仕様の本質」— 全ての行が実際に書かれるコードに対応する -->
<!-- How（実装方法）は書かない。What（振る舞い）だけを書く -->

## 完了条件

- [ ] 振る舞い仕様の全行が実装されている
- [ ] 自動テストが全てパスしている
- [ ] CI が green である
- [ ] {追加の実装視点の条件があれば}

## テスト設計

### 自動テスト（TDD の起点）

**ユニットテスト（テストの大部分をここに）:**
- [ ] {テスト対象と期待する振る舞い。振る舞い仕様の各行に対応}

**統合テスト（外部依存がある場合のみ）:**
- [ ] {DB・外部API等との連携テスト。必要な場合のみ}

**E2Eテスト（最小限）:**
- [ ] {エンドツーエンドで検証が必要な場合のみ。ほとんどの Task では不要}

<!-- テストピラミッド: ユニットテストを大部分にし、統合テストは少量、E2Eは最小限 -->
<!-- 「テストが通れば、そのときコーディングが終了」— テストがないコードは存在しないのと同じ -->

### 受入確認（手動）

- [ ] {PdO/QA が手動で確認するシナリオ。画面の見た目・操作感・文言等、自動テストでカバーしにくい観点}

<!-- 探索的テストの入口: 自動テストでカバーしきれない想定外のケースを発見する -->
<!-- 全ての受入確認が OK + CI green = Task 完了 -->

## 依存

{blocked by #XX（あれば。なければ「なし」）}

## 技術メモ

{CodingAgent がコードベースを探索する起点となる情報を書く}

- **対象モジュール/ファイル**: {変更が必要なモジュールやファイルパス。例: `src/features/<feature-name>/`}
- **参考にすべき既存実装**: {同様のパターンで実装済みのコード。例: 「認証モジュールの実装パターンに倣う」}
- **関連 ADR**: {設計判断の背景。例: `docs/adrs/<adr-id>`}
- **注意点**: {既知のハマりポイント、触る際の制約。なければ省略}

<!-- CodingAgent はコンテキストゼロの状態からこの Task に着手する。「どこから手をつけるか」がわかる情報を必ず書く -->
